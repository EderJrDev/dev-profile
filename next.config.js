/** @type {import('next').NextConfig} */
// const nextConfig = {}

module.exports = {
  images: {
    domains: ['avatars.githubusercontent.com'], // Adicione todos os domínios que você deseja permitir para carregar imagens.
  },
}
